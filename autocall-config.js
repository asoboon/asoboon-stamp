/*
 * ASOBooN 入場受付 / 継続AUTO 自動呼出
 * Persistent AUTO v0.9
 *
 * AirWAIT APIキー設定
 * 指定コード設定済み
 * GAS URL設定済み
 *
 * STAFF_KEY はGitHubには入れません。
 * スタッフ画面で端末に1回だけ保存します。
 */

window.ASOBOON_RECEPTION_CONFIG = Object.freeze({

  // AirWAIT
  airwaitApiKey: "ここにAPIキー",

  // AirWAIT 指定コード
  airwaitStoreId: "KR01205179",

  // ASOBooN AirWAIT 台帳 GAS
  ledgerWebAppUrl:
    "https://script.google.com/macros/s/AKfycbzpU0Tkz8U-HRxH0iFdjxZ3ZajkRkVRO4cFsG1dWA16RQfKbyJEtxP0tgPwjj_yrk8eNw/exec",


  // =====================================================
  // 受付時間
  // =====================================================

  // 19:00以降は翌日受付
  nextDayOpenHour: 19,

  // 18:00〜18:59は受付休止
  receptionCloseHour: 18,


  // =====================================================
  // 料金・人数
  // =====================================================

  priceAdult: 600,

  priceChild: 900,

  maxTotalPeople: 10,

  childrenPerAdult: 3,


  // =====================================================
  // 表示しないAirWAIT待ち項目
  // =====================================================

  blockedWaitTypeIds: [
    "0042"
  ],

  blockedNamePatterns: [
    "テスト",
    "ご待機者専用",
    "待機者専用"
  ],


  // =====================================================
  // お客様画面
  // =====================================================

  customerRefreshMs: 15000,


  // =====================================================
  // スタッフAUTO
  // =====================================================

  // AirWAIT確認周期
  staffRefreshMs: 10000,

  // AirWAITと共有台帳の状態が
  // この時間以内に取得できている時だけAUTO許可
  maxFreshAgeMs: 20000,


  // =====================================================
  // 自動呼出
  // =====================================================

  // 各回で「呼出中」を最大10組に維持
  autoCallingPool: 10,

  // 連続して呼ぶ時の間隔
  autoCallGapMs: 1200,


  // =====================================================
  // 18:00以降
  // =====================================================

  /*
   * 18:00でAUTOをOFFにはしません。
   *
   * 呼出処理だけ休止します。
   *
   * AUTO ON状態はGASに保存され、
   * 翌日もそのまま維持されます。
   *
   * 翌日の各受付回の開始時刻になると
   * 自動的に呼出を再開します。
   *
   * スタッフがAUTO OFFを押した時だけ
   * 継続AUTOが停止します。
   */

  autoStopTime: "18:00",


  // =====================================================
  // 受付回の開始時刻
  // =====================================================

  /*
   * 基本はAirWAITの待ち項目名から自動判定します。
   *
   * 例
   *
   * 10:00の回
   * 12:30〜
   * 14:00の回
   * 13時30分
   * 10時半
   *
   * 名前から時刻を判断できない特殊枠だけ、
   * waitTypeIdをここへ追加します。
   */

  slotStartOverrides: {

    // 例
    // "0001": "10:00",
    // "0002": "12:30"

  }

});
